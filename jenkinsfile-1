pipeline {
    agent any
    environment{
         PATH =  "/usr/bin:$PATH"
    }
    stages
         stage("build code"){
            steps{
               sh "mvn clean install"
      }
   }
}     
